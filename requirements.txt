# Pinned to the exact versions that trained/pickled the models in models/.
# scikit-learn / numpy pickles are version-sensitive: deploying with an older
# stack (e.g. sklearn 1.5.0) breaks model loading with
# "'LogisticRegression' object has no attribute 'multi_class'".
flask==3.0.3
Werkzeug==3.0.3
gunicorn==21.2.0

# Scientific stack — must match the training environment (see models/)
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
scikit-learn==1.9.0
joblib==1.4.2
threadpoolctl==3.6.0

matplotlib==3.10.9
seaborn==0.13.2
reportlab==4.2.0

# Packet capture parsing (PCAP -> CSV feature extraction)
scapy==2.7.0

# Parquet support (only needed if training on .parquet datasets)
pyarrow==15.0.2
